{
  "name": "create-astro-ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "lint": "eslint src/",
    "prettify": "prettier -w src/ --plugin=prettier-plugin-astro",
    "typecheck": "tsc --noEmit",
    "prettycheck": "prettier -c src/ --plugin=prettier-plugin-astro",
    "test": "npm run typecheck && npm run lint && npm run prettycheck"
  },
  "dependencies": {
    "astro": "^4.5.12"
  },
  "devDependencies": {
    "@astrojs/check": "^0.5.10",
    "@mgcrea/eslint-config-astro": "^0.10.1",
    "eslint": "^8.57.0",
    "prettier-plugin-astro": "^0.13.0",
    "prettier-plugin-organize-imports": "^3.2.4",
    "prettier": "^3.2.5",
    "sharp": "^0.33.3",
    "typescript": "^5.4.3"
  }
}
